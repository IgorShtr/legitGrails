<div class="card card-article critical-clear {% for btag in article.tags %}{{btag | handle }}{% endfor %}" data-animate data-title="{{ article.title }}">
  <a href="{{ article.url }}" class="card__wrapper card--article">
    {% if article.image %}
      {% assign article_image = article.image %}
    {% else %}
      {% assign article_image = article.products.first %}
    {% endif %}

    {% if article_image != blank %}
      {% include 'card-image', type: article_image, grid_style: grid_style %}
    {% endif %}

    <div class="card__info">
      {% if section.settings.blog_show_date %}
        <span class="card__date text-small--uppercase font-title">
          {{ article.published_at | time_tag: format: 'date' }}
        </span>
      {% endif %}
      <h3 class="card__name font-title">{{ article.title }}</h3>
      <div class="card__excerpt rte font-description">
        {% if article.excerpt.size > 0 %}
          {{ article.excerpt }}
        {% else %}
          {{ article.content | strip_html | truncatewords: 30 }}
        {% endif %}
      </div>
      <span class="card__read-more font-title">
        {{ 'blogs.article.read_more' | t }}
        {% include 'icon-arrow-right' %}
      </span>
    </div>
  </a>
</div>
